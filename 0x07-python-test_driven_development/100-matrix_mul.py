#!/usr/bin/python3
def matrix_mul(m_a, m_b):
result = []
   for i in range(len(m_a)):
       for j in range(len(m_n[0])):
           for k in range(len m_b)):
               result[i][j] += A[i][k] * B[k][j]
               result.append (result[i][j])
return result
